{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the NodeDesk main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

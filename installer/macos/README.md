# installer/macos/

Planned artifact: `NodeDesk.dmg` (via Tauri bundler), controller mode first.

macOS host support depends on Sunshine's macOS maturity; controller-only is
the realistic first step. Requires Developer ID signing + notarization for
distribution.

**Status:** planned.
